//Tuples are a way to store multiple values in a single variable. Tuples are similar to arrays but have a fixed number of elements and the types of elements are known.
const color: [number, number, number] = [1, 1, 1];
